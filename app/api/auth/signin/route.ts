import {NextResponse} from "next/server";import {createSession,ensureAuth,passwordMatches} from "../../../auth";import {env} from "../../../../db/runtime";
export async function POST(request:Request){await ensureAuth();const body=await request.json() as Record<string,unknown>;const email=String(body.email||"").trim().toLowerCase(),password=String(body.password||"");const account=await env.DB.prepare("SELECT id,password_hash AS passwordHash FROM auth_accounts WHERE email=?").bind(email).first<{id:number;passwordHash:string}>();if(!account||!passwordMatches(password,account.passwordHash))return NextResponse.json({error:"The email or password is not correct."},{status:401});const secure=request.headers.get("x-forwarded-proto")==="https"||new URL(request.url).protocol==="https:";await createSession(account.id,secure);return NextResponse.json({ok:true})}


