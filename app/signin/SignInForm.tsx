"use client";import {useState} from "react";
export default function SignInForm({returnTo}:{returnTo:string}){const [mode,setMode]=useState<"signin"|"signup">("signin"),[form,setForm]=useState({fullName:"",email:"",password:""}),[message,setMessage]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();setMessage("Please wait…");const response=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)}),data=await response.json();if(response.ok){setMessage(mode==="signup"?"Account created successfully. You are now signed in.":"Sign-in successful. Welcome back.");setTimeout(()=>{location.href=returnTo||"/"},900)}else setMessage(data.error||"Please try again.")};return <main className="nativeAuth"><section><a href="/" className="authBrand">V <span>VISIT PAPUA NEW GUINEA</span></a><p className="eyebrow">{mode==="signin"?"WELCOME BACK":"CREATE YOUR ACCOUNT"}</p><h1>{mode==="signin"?"Sign in to Visit PNG":"Start planning your PNG journey"}</h1><p>Your trips, saved places, bookings and rewards stay together on your Visit PNG account.</p><form onSubmit={submit}>{mode==="signup"&&<label>Full name<input required autoComplete="name" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label>}<label>Email address<input required type="email" autoComplete="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input required minLength={10} type="password" autoComplete={mode==="signin"?"current-password":"new-password"} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button>{mode==="signin"?"Sign in":"Create account"}</button></form>{message&&<p className="formStatus" aria-live="polite">{message}</p>}<button className="authSwitch" onClick={()=>{setMode(mode==="signin"?"signup":"signin");setMessage("")}}>{mode==="signin"?"New to Visit PNG? Create an account":"Already have an account? Sign in"}</button></section></main>}

