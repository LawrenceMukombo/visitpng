"use client";
import {useCallback,useEffect,useState} from "react";
type Viewer={signedIn:true;displayName:string;email:string;signOutPath:string}|{signedIn:false;signInPath:string};
type Category={slug:string;name:string;icon:string};type Listing={id:number;slug:string;name:string;summary:string;imageUrl:string;tag:string;currency:string;basePrice:number;memberPrice:number|null;rating:number;reviewCount:number;destination:string;province:string;categoryName:string;providerName:string;sourceUrl:string|null};type Catalogue={categories:Category[];listings:Listing[];meta:{count:number}};
type PublicReview={id:number;overallRating:number;valueRating:number|null;serviceRating:number|null;safetyRating:number|null;accessibilityRating:number|null;title:string;body:string;travelType:string|null;dateOfExperience:string|null;verificationType:string;providerResponse:string|null;providerRespondedAt:string|null;createdAt:string;author:string};type MyReview={id:number;listingId:number;listingName:string;imageUrl:string;overallRating:number;title:string;body:string;verificationType:string;moderationStatus:string;moderationReason:string|null;createdAt:string};type Booking={id:number;reference:string;status:string;currency:string;subtotal:number;total:number;startDate:string;endDate:string;guestCount:number;contactName:string;contactMobile:string|null;holdExpiresAt:string|null;isTestData:number;createdAt:string;listingId:number;listingName:string;quantity:number;unitPrice:number;imageUrl:string;destination:string;paymentStatus:string|null;paymentProvider:string|null};
type Profile={id:number;email:string;fullName:string|null;preferredName:string|null;mobile:string|null;country:string;preferredLanguage:string;role:string;status:string};type Audit={action:string;entityType:string;details:string;createdAt:string};type SavedItem=Listing&{id:number;listingId:number;note:string|null;createdAt:string};type Wishlist={id:number;name:string;privacy:string;shareCode:string|null;items:SavedItem[]};type TripItem={id:number;listingId:number|null;title:string;itemType:string;scheduledDate:string;startTime:string|null;endTime:string|null;cost:number;bookingReference:string|null;notes:string|null;position:number;imageUrl:string|null;destination:string|null};type Trip={id:number;name:string;destination:string;startDate:string;endDate:string;travellerCount:number;budget:number;interests:string|null;notes:string|null;shareCode:string|null;status:string;items:TripItem[];collaborators:{id:number;email:string;role:string;status:string}[]};type Benefit={id:number;code:string;name:string;description:string;usageLimit:number|null};type Plan={id:number;code:string;name:string;audience:string;billingPeriod:string;price:number;currency:string;description:string;isComplimentary:number;benefits:Benefit[]};type MembershipData={plans:Plan[];subscription:null|{id:number;memberNumber:string;status:string;startDate:string;expiryDate:string|null;autoRenew:number;cancelAtPeriodEnd:number;planId:number;planName:string;planCode:string};points:number;history:{token:string;status:string;redeemedAt:string|null;createdAt:string;benefitName:string}[];memberName:string};
export default function VisitPngApp({viewer}:{viewer:Viewer}){const[data,setData]=useState<Catalogue|null>(null);const[error,setError]=useState("");const[loading,setLoading]=useState(true);const[q,setQ]=useState("");const[category,setCategory]=useState("all");const[selected,setSelected]=useState<Listing|null>(null);const[saveListing,setSaveListing]=useState<Listing|null>(null);const[bookingListing,setBookingListing]=useState<Listing|null>(null);const[reviewListing,setReviewListing]=useState<Listing|null>(null);const[tab,setTab]=useState<"Explore"|"Bookings"|"Reviews"|"Saved"|"Trips"|"Membership"|"Profile">("Explore");useEffect(()=>{if(new URLSearchParams(window.location.search).has("wishlist"))setTab("Saved")},[]);const load=useCallback(async(signal?:AbortSignal)=>{setLoading(true);setError("");try{const p=new URLSearchParams();if(q.trim())p.set("q",q.trim());if(category!=="all")p.set("category",category);const r=await fetch(`/api/catalogue?${p}`,{signal});if(!r.ok)throw new Error();setData(await r.json())}catch(e){if((e as Error).name!=="AbortError")setError("We could not load places. Please try again.")}finally{if(!signal?.aborted)setLoading(false)}},[q,category]);useEffect(()=>{const c=new AbortController();const t=setTimeout(()=>load(c.signal),250);return()=>{clearTimeout(t);c.abort()}},[load]);return <main className="app"><Header viewer={viewer} profile={()=>setTab("Profile")}/>{tab==="Explore"?<><section className="hero"><p className="eyebrow lime">THE LAND OF A MILLION JOURNEYS</p><h1>Find the PNG<br/>you’ll never forget.</h1><p>Discover trusted places and plan your trip with confidence.</p><div className="search"><label htmlFor="catalogue-search">Where do you want to go?</label><div><span>⌕</span><input id="catalogue-search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search places, provinces or experiences"/><button onClick={()=>setQ("")}>×</button></div><nav><button className={category==="all"?"selectedFilter":""} onClick={()=>setCategory("all")}>All places</button><button onClick={()=>setTab("Bookings")}>My bookings</button></nav></div></section><section className="content"><div className="cats">{data?.categories.map(c=><button key={c.slug} className={category===c.slug?"active":""} onClick={()=>setCategory(category===c.slug?"all":c.slug)}><span>{c.icon}</span>{c.name}</button>)}</div><aside className="notice"><i>i</i><div><b>{viewer.signedIn?`Welcome, ${viewer.displayName}`:"Browse freely. Sign in when you’re ready."}</b><small>{viewer.signedIn?"You are signed in and ready to plan.":"Create a Visit PNG account to keep your plans together."}</small></div></aside><div className="title"><div><p className="eyebrow">PLACES TO EXPLORE</p><h2>{q?`Results for “${q}”`:"Discover something remarkable"}</h2></div>{data&&<span className="resultCount">{data.meta.count} found</span>}</div>{loading?<Loading/>:error?<ErrorState message={error} retry={()=>load()}/>:data&&data.listings.length?<div className="cards">{data.listings.map(p=><Card key={p.id} listing={p} open={setSelected} save={()=>viewer.signedIn?setSaveListing(p):setTab("Profile")}/>)}</div>:<Empty clear={()=>{setQ("");setCategory("all")}}/>}<ModuleStatus openReviews={()=>setTab("Reviews")}/></section></>:tab==="Bookings"?<BookingsScreen viewer={viewer}/>:tab==="Reviews"?<ReviewsScreen viewer={viewer}/>:tab==="Saved"?<SavedScreen viewer={viewer}/>:tab==="Trips"?<TripsScreen viewer={viewer}/>:tab==="Membership"?<MembershipScreen viewer={viewer}/>:<ProfileScreen viewer={viewer}/>}<Bottom tab={tab} setTab={setTab}/>{selected&&<Details listing={selected} close={()=>setSelected(null)} book={()=>{setSelected(null);viewer.signedIn?setBookingListing(selected):setTab("Profile")}} review={()=>{setSelected(null);viewer.signedIn?setReviewListing(selected):setTab("Profile")}}/>} {reviewListing&&viewer.signedIn&&<ReviewSheet listing={reviewListing} close={()=>setReviewListing(null)} done={()=>{setReviewListing(null);setTab("Reviews")}}/>}{bookingListing&&viewer.signedIn&&<BookingSheet listing={bookingListing} close={()=>setBookingListing(null)} openBookings={()=>{setBookingListing(null);setTab("Bookings")}}/>}{saveListing&&viewer.signedIn&&<SaveSheet listing={saveListing} close={()=>setSaveListing(null)}/>}</main>}
function Header({viewer,profile}:{viewer:Viewer;profile:()=>void}){const initials=viewer.signedIn?viewer.displayName.split(/\s|@/).filter(Boolean).slice(0,2).map(x=>x[0]).join("").toUpperCase():"GU";return <header><button className="brand"><i>V</i><span>VISIT<br/><b>PAPUA NEW GUINEA</b></span></button><nav><button aria-label="Signed-in connection">●</button>{viewer.signedIn?<button className="avatar" onClick={profile}>{initials}</button>:<a className="signInMini" href={viewer.signInPath}>Sign in</a>}</nav></header>}
function ProfileScreen({viewer}:{viewer:Viewer}){if(!viewer.signedIn)return <section className="accountGuest"><div className="accountMark">V</div><p className="eyebrow">YOUR ACCOUNT</p><h1>Your journeys, in one secure place.</h1><p>Sign in to create your traveller profile. Use your Visit PNG email and password to keep your journeys together.</p><a href={viewer.signInPath}>Sign in to Visit PNG</a><small>You can still browse places without signing in.</small></section>;return <AccountForm viewer={viewer}/>}
function MembershipScreen({viewer}:{viewer:Viewer}){const[data,setData]=useState<MembershipData|null>(null);const[status,setStatus]=useState("Loading membership…");const load=useCallback(()=>{if(!viewer.signedIn)return;fetch("/api/membership").then(r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>{setData(x);setStatus("")}).catch(()=>setStatus("Membership could not be loaded."))},[viewer]);useEffect(()=>load(),[load]);const testPay=async(id:number)=>{setStatus("Trying the practice payment…");const r=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"testMembership",subscriptionId:id})});const x=await r.json();if(r.ok){setStatus("Practice payment finished. No real money was charged.");load()}else setStatus(x.error||"The practice payment did not work.")};const act=async(body:Record<string,unknown>)=>{setStatus("Saving…");const r=await fetch("/api/membership",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const x=await r.json();if(r.ok){setData(x);setStatus("Membership saved.")}else setStatus(x.error||"Membership update failed.")};if(!viewer.signedIn)return <section className="accountGuest"><div className="accountMark">★</div><p className="eyebrow">MEMBERSHIP</p><h1>More access. More PNG.</h1><p>Sign in to compare plans, manage your membership card and track reward points.</p><a href={viewer.signInPath}>Sign in to view membership</a></section>;const currentPlan=data?.plans.find(p=>p.id===data.subscription?.planId);const usable=data?.subscription&&["active","complimentary"].includes(data.subscription.status);return <section className="saved membershipPage"><p className="eyebrow">MEMBERSHIP & LOYALTY</p><h1>Belong to every journey.</h1><p>Choose the membership that suits you. Practice payments never charge real money.</p>{status&&<p className="formStatus" aria-live="polite">{status}</p>}{data?.subscription&&<article className="memberCard"><div className="memberCardTop"><span>VISIT PNG</span><b>{data.subscription.status.replaceAll("_"," ")}</b></div><p>MEMBER</p><h2>{data.memberName}</h2><strong>{data.subscription.memberNumber}</strong><div className="memberCardBottom"><span>{data.subscription.planName}</span><span>{data.subscription.expiryDate?`Valid to ${new Date(data.subscription.expiryDate).toLocaleDateString()}`:"No expiry"}</span></div><div className="memberCode" aria-label={`Membership member code ${data.subscription.memberNumber}`}><i/><i/><i/><i/><i/><i/><i/><i/><i/></div></article>}<div className="loyaltySummary"><div><small>Reward balance</small><strong>{data?.points??0}</strong><span>points</span></div><p>Earn more points through eligible bookings, renewals, trips and reviews.</p></div><div className="plans"><div className="title"><div><p className="eyebrow">MEMBERSHIP OPTIONS</p><h2>Choose your membership</h2></div></div>{data?.plans.map(plan=><article className={`planCard ${data.subscription?.planId===plan.id?"current":""}`} key={plan.id}><header><div><small>{plan.audience} · {plan.billingPeriod}</small><h2>{plan.name}</h2></div><strong>{plan.price?`K${plan.price}`:"Free"}<small>{plan.price?`/${plan.billingPeriod}`:""}</small></strong></header><p>{plan.description}</p><ul>{plan.benefits.map(b=><li key={b.id}><span>✓</span><div><b>{b.name}</b><small>{b.description}</small></div></li>)}</ul><button disabled={data.subscription?.planId===plan.id&&data.subscription.status!=="cancelled"} onClick={()=>confirm(plan.price?"Choose this membership? It will wait for a practice payment, and no real money will be taken.":"Start this free membership?")&&act({action:"select",planId:plan.id})}>{data.subscription?.planId===plan.id?"Current plan":plan.price?"Select plan":"Activate free plan"}</button></article>)}</div>{data?.subscription&&<section className="benefitsPanel"><div className="title"><div><p className="eyebrow">CURRENT BENEFITS</p><h2>{usable?"Ready when you are":"Choose a membership first"}</h2></div></div>{data.subscription.status==="payment_due"&&<aside className="membershipNotice"><b>Practice payment needed</b><p>Your plan choice is saved. Try the practice payment below. No real money is charged.</p><button onClick={()=>data.subscription&&testPay(data.subscription.id)}>Try practice payment</button></aside>}<div className="benefitGrid">{currentPlan?.benefits.map(b=><article key={b.id}><b>{b.name}</b><p>{b.description}</p><button disabled={!usable} onClick={()=>act({action:"benefit",benefitId:b.id})}>Use this benefit</button></article>)}</div>{data.history.length>0&&<div className="tokenHistory"><h3>Benefits used</h3>{data.history.map(x=><div key={x.token}><span>{x.benefitName}</span><code>{x.token}</code><small>{x.status}</small></div>)}</div>}<button className="cancelMembership" onClick={()=>confirm("Cancel this membership?")&&act({action:"cancel"})}>Cancel membership</button></section>}</section>}function TripsScreen({viewer}:{viewer:Viewer}){const[trips,setTrips]=useState<Trip[]>([]);const[active,setActive]=useState(0);const[status,setStatus]=useState("Loading trips…");const[form,setForm]=useState({name:"",destination:"",startDate:"",endDate:"",travellerCount:"1",budget:"",interests:""});const[item,setItem]=useState({title:"",itemType:"activity",scheduledDate:"",startTime:"",endTime:"",cost:"",bookingReference:"",notes:""});const load=useCallback(()=>{if(!viewer.signedIn)return;fetch("/api/trips").then(r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>{setTrips(x.trips);setStatus("")}).catch(()=>setStatus("Trips could not be loaded."))},[viewer]);useEffect(()=>load(),[load]);const mutate=async(method:string,body:Record<string,unknown>)=>{setStatus("Saving…");const r=await fetch("/api/trips",{method,headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const x=await r.json();if(r.ok){setTrips(x.trips);setStatus("Trip saved.")}else setStatus(x.error||"Trip update failed.")};if(!viewer.signedIn)return <section className="accountGuest"><div className="accountMark">▣</div><p className="eyebrow">YOUR TRIPS</p><h1>Turn inspiration into a journey.</h1><p>Sign in to build day-by-day itineraries, budgets and shared travel plans.</p><a href={viewer.signInPath}>Sign in to plan a trip</a></section>;const current=trips.find(t=>t.id===active)||trips[0];const dates=current?Array.from({length:Math.min(60,Math.max(1,Math.floor((new Date(current.endDate).getTime()-new Date(current.startDate).getTime())/86400000)+1))},(_,i)=>new Date(new Date(current.startDate+"T00:00:00").getTime()+i*86400000).toISOString().slice(0,10)):[];const spent=current?.items.reduce((n,x)=>n+x.cost,0)||0;const download=()=>{if(!current)return;const blob=new Blob([JSON.stringify({generatedAt:new Date().toISOString(),trip:current},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`${current.name.toLowerCase().replace(/[^a-z0-9]+/g,"-")}-saved-trip.json`;a.click();URL.revokeObjectURL(a.href)};return <section className="saved tripsPage"><p className="eyebrow">TRIP PLANNER</p><h1>Plan the whole journey.</h1><p>Create a trip, shape each day, track estimated costs and bring others into the plan.</p><form className="tripCreate" onSubmit={e=>{e.preventDefault();mutate("POST",{action:"create",...form});setForm({name:"",destination:"",startDate:"",endDate:"",travellerCount:"1",budget:"",interests:""})}}><input required placeholder="Trip name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required placeholder="Destination" value={form.destination} onChange={e=>setForm({...form,destination:e.target.value})}/><label>Starts<input required type="date" value={form.startDate} onChange={e=>setForm({...form,startDate:e.target.value})}/></label><label>Ends<input required type="date" min={form.startDate} value={form.endDate} onChange={e=>setForm({...form,endDate:e.target.value})}/></label><input type="number" min="1" aria-label="Travellers" placeholder="Travellers" value={form.travellerCount} onChange={e=>setForm({...form,travellerCount:e.target.value})}/><input type="number" min="0" aria-label="Budget in kina" placeholder="Budget (K)" value={form.budget} onChange={e=>setForm({...form,budget:e.target.value})}/><input className="wide" placeholder="Interests, comma separated" value={form.interests} onChange={e=>setForm({...form,interests:e.target.value})}/><button className="wide">Create trip</button></form>{status&&<p className="formStatus" aria-live="polite">{status}</p>}{trips.length>0&&<div className="tripTabs">{trips.map(t=><button key={t.id} className={current?.id===t.id?"active":""} onClick={()=>setActive(t.id)}><strong>{t.name}</strong><small>{t.startDate} → {t.endDate}</small></button>)}</div>}{current?<><div className="tripHero"><p className="eyebrow">{current.status.toUpperCase()} · {current.destination}</p><h2>{current.name}</h2><div className="tripStats"><span><b>{current.travellerCount}</b> travellers</span><span><b>{current.items.length}</b> plans</span><span><b>K{spent}</b> of K{current.budget}</span></div><progress value={spent} max={Math.max(current.budget,spent,1)}/><div className="tripTools"><button onClick={()=>{const value=prompt("Update trip budget (K)",String(current.budget));if(value!==null)mutate("PATCH",{tripId:current.id,budget:Number(value)})}}>Budget</button><button onClick={()=>{const email=prompt("Travel companion email");if(email){const role=prompt("Role: view, comment, suggest or edit","view")||"view";mutate("POST",{action:"invite",tripId:current.id,email,role})}}}>Invite</button><button onClick={download}>Save trip copy</button><button onClick={()=>confirm("Delete this trip?")&&mutate("DELETE",{tripId:current.id})}>Delete</button></div>{current.collaborators.length>0&&<p className="collaborators">Travelling with {current.collaborators.map(c=>`${c.email} (${c.role})`).join(", ")}</p>}</div><form className="itemCreate" onSubmit={e=>{e.preventDefault();mutate("POST",{action:"addItem",tripId:current.id,...item});setItem({...item,title:"",cost:"",bookingReference:"",notes:""})}}><h2>Add to the itinerary</h2><input required placeholder="Activity, meal, transport or free time" value={item.title} onChange={e=>setItem({...item,title:e.target.value})}/><select value={item.itemType} onChange={e=>setItem({...item,itemType:e.target.value})}><option value="activity">Activity</option><option value="accommodation">Accommodation</option><option value="transport">Transport</option><option value="meal">Meal</option><option value="free_time">Free time</option><option value="personal">Personal</option></select><input required type="date" min={current.startDate} max={current.endDate} value={item.scheduledDate} onChange={e=>setItem({...item,scheduledDate:e.target.value})}/><input aria-label="Start time" type="time" value={item.startTime} onChange={e=>setItem({...item,startTime:e.target.value})}/><input aria-label="End time" type="time" value={item.endTime} onChange={e=>setItem({...item,endTime:e.target.value})}/><input type="number" min="0" aria-label="Estimated cost" placeholder="Cost (K)" value={item.cost} onChange={e=>setItem({...item,cost:e.target.value})}/><input placeholder="Booking reference (optional)" value={item.bookingReference} onChange={e=>setItem({...item,bookingReference:e.target.value})}/><input placeholder="Notes" value={item.notes} onChange={e=>setItem({...item,notes:e.target.value})}/><button>Add plan</button></form><div className="itinerary">{dates.map((date,day)=>{const plans=current.items.fil…1597 tokens truncated…ToTrip(item)}>Add to trip</button><button onClick={()=>{const note=prompt("Add a note",item.note||"");if(note!==null)mutate("PATCH",{itemId:item.id,note})}}>Note</button>{lists.length>1&&<select aria-label={`Move ${item.name}`} value={current.id} onChange={e=>mutate("PATCH",{itemId:item.id,wishlistId:Number(e.target.value)})}>{lists.map(l=><option value={l.id} key={l.id}>{l.name}</option>)}</select>}<button onClick={()=>mutate("DELETE",{itemId:item.id})}>Remove</button></div>}</div></article>)}</div>:<div className="empty compact"><h2>No places saved yet</h2><p>Tap the heart on any listing to add it here.</p></div>}</>}</section>}
function SaveSheet({listing,close}:{listing:Listing;close:()=>void}){const[lists,setLists]=useState<Wishlist[]>([]);const[status,setStatus]=useState("Loading your wishlists…");useEffect(()=>{fetch("/api/wishlists").then(r=>r.json()).then(x=>{setLists(x.wishlists||[]);setStatus("")}).catch(()=>setStatus("Wishlists unavailable."))},[]);const save=async(id:number)=>{setStatus("Saving…");const r=await fetch("/api/wishlists",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"add",wishlistId:id,listingId:listing.id})});if(r.ok){const x=await r.json();localStorage.setItem("visitpng-wishlists",JSON.stringify(x.wishlists));setStatus("Saved to your wishlist.");setTimeout(close,600)}else setStatus("Could not save this place.")};return <div className="overlay" onClick={close}><article className="sheet saveSheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={close}>×</button><p className="eyebrow">SAVE FOR LATER</p><h2>{listing.name}</h2><p>Choose a wishlist.</p>{lists.map(l=><button className="chooseList" key={l.id} onClick={()=>save(l.id)}><span>{l.name}</span><b>{l.items.length} saved</b></button>)}{status&&<p className="formStatus" aria-live="polite">{status}</p>}</article></div>}function AccountForm({viewer}:{viewer:Extract<Viewer,{signedIn:true}>}){const[profile,setProfile]=useState<Profile|null>(null);const[audit,setAudit]=useState<Audit[]>([]);const[status,setStatus]=useState("Loading your account…");useEffect(()=>{fetch("/api/profile").then(r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>{setProfile(x.profile);setAudit(x.audit);setStatus("")}).catch(()=>setStatus("We could not open your account."))},[]);const save=async(e:React.FormEvent)=>{e.preventDefault();if(!profile)return;setStatus("Saving…");const r=await fetch("/api/profile",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(profile)});if(r.ok){const x=await r.json();setProfile(x.profile);setStatus("Profile saved.")}else setStatus("Profile update failed.")};return <section className="profilePage"><div className="profileHero"><span>{viewer.displayName[0]?.toUpperCase()}</span><div><p className="eyebrow lime">YOUR TRAVELLER ACCOUNT</p><h1>{profile?.preferredName||viewer.displayName}</h1><p>{viewer.email}</p></div></div>{profile?<form onSubmit={save}><div className="accountState"><span>● Active</span><span>{profile.role}</span></div><label>Preferred name<input value={profile.preferredName??""} onChange={e=>setProfile({...profile,preferredName:e.target.value})}/></label><label>Mobile number<input value={profile.mobile??""} onChange={e=>setProfile({...profile,mobile:e.target.value})} inputMode="tel"/></label><label>Country of residence<input value={profile.country} onChange={e=>setProfile({...profile,country:e.target.value})}/></label><label>Preferred language<select value={profile.preferredLanguage} onChange={e=>setProfile({...profile,preferredLanguage:e.target.value})}><option>English</option><option>Tok Pisin</option><option>Hiri Motu</option></select></label><button type="submit">Save profile</button>{status&&<p className="formStatus" aria-live="polite">{status}</p>}</form>:<div className="accountLoading">{status}</div>}<div className="securityCard"><p className="eyebrow">SECURITY & ACCESS</p><h2>Your account is protected</h2><p>Your sign-in is protected and your password is safely locked.</p><a href={viewer.signOutPath}>Sign out</a></div>{audit.length>0&&<div className="audit"><p className="eyebrow">RECENT ACTIVITY</p>{audit.map((a,i)=><div key={i}><span>{a.action.replaceAll("_"," ")}</span><time>{new Date(a.createdAt).toLocaleString()}</time></div>)}</div>}</section>}
function Card({listing:p,open,save}:{listing:Listing;open:(p:Listing)=>void;save:()=>void}){return <article className="card" onClick={()=>open(p)}><div className="pic" style={{backgroundImage:`url(${p.imageUrl})`}}><span>{p.tag}</span><button aria-label={`Save ${p.name}`} onClick={e=>{e.stopPropagation();save()}}>♡</button></div><div className="cardBody"><div className="meta"><span>{p.categoryName} · {p.destination}</span><b>★ {p.rating}</b></div><h3>{p.name}</h3><p>{p.summary}</p><footer><div><small>Sample member price</small><strong>K{p.memberPrice??p.basePrice}</strong> <del>K{p.basePrice}</del></div><em>Sample listing</em></footer></div></article>}
function BookingSheet({listing,close,openBookings}:{listing:Listing;close:()=>void;openBookings:()=>void}){const today=new Date().toISOString().slice(0,10);const tomorrow=new Date(Date.now()+86400000).toISOString().slice(0,10);const[form,setForm]=useState({startDate:today,endDate:tomorrow,guestCount:"1",contactName:"",contactMobile:""});const[status,setStatus]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();setStatus("Checking available places…");const r=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"create",listingId:listing.id,...form})});const x=await r.json();if(r.ok){setStatus(`Held for 15 minutes. Reference ${x.reference}.`);setTimeout(openBookings,900)}else setStatus(x.error||"Booking could not be held.")};return <div className="overlay" onClick={close}><article className="sheet bookingSheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={close}>×</button><p className="eyebrow">BOOK YOUR VISIT</p><h2>{listing.name}</h2><aside className="testGateway"><b>Practice payment only</b><p>This sample listing uses a practice payment. We do not ask for card details, and no real money is charged.</p></aside><form onSubmit={submit}><label>Arrival / activity date<input required type="date" min={today} value={form.startDate} onChange={e=>setForm({...form,startDate:e.target.value,endDate:e.target.value>=form.endDate?new Date(new Date(e.target.value+"T00:00:00Z").getTime()+86400000).toISOString().slice(0,10):form.endDate})}/></label><label>Departure / end date<input required type="date" min={new Date(new Date(form.startDate+"T00:00:00Z").getTime()+86400000).toISOString().slice(0,10)} value={form.endDate} onChange={e=>setForm({...form,endDate:e.target.value})}/></label><label>Guests<input required type="number" min="1" max="12" value={form.guestCount} onChange={e=>setForm({...form,guestCount:e.target.value})}/></label><label>Contact name<input required maxLength={100} value={form.contactName} onChange={e=>setForm({...form,contactName:e.target.value})}/></label><label>Mobile (optional)<input inputMode="tel" maxLength={30} value={form.contactMobile} onChange={e=>setForm({...form,contactMobile:e.target.value})}/></label><div className="bookingEstimate"><span>Seeded rate, per day</span><strong>K{listing.memberPrice??listing.basePrice}</strong></div><button>Check availability & hold</button></form>{status&&<p className="formStatus" aria-live="polite">{status}</p>}</article></div>}
function BookingsScreen({viewer}:{viewer:Viewer}){const[bookings,setBookings]=useState<Booking[]>([]);const[status,setStatus]=useState("Loading bookings…");const load=useCallback(()=>{if(!viewer.signedIn)return;fetch("/api/bookings").then(r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>{setBookings(x.bookings||[]);setStatus("")}).catch(()=>setStatus("Bookings could not be loaded."))},[viewer]);useEffect(()=>load(),[load]);const act=async(action:string,bookingId:number)=>{setStatus("Updating booking…");const r=await fetch("/api/bookings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action,bookingId})});const x=await r.json();if(r.ok){setBookings(x.bookings);setStatus(action==="testConfirm"?"Practice payment finished. Your booking is confirmed, and no real money was charged.":"Booking cancelled.")}else setStatus(x.error||"Booking update failed.")};if(!viewer.signedIn)return <section className="accountGuest"><div className="accountMark">▤</div><p className="eyebrow">YOUR BOOKINGS</p><h1>Your reservations, safely together.</h1><p>Sign in to hold dated inventory and manage booking confirmations.</p><a href={viewer.signInPath}>Sign in to view bookings</a></section>;return <section className="saved bookingsPage"><p className="eyebrow">YOUR BOOKINGS & PAYMENTS</p><h1>Your PNG bookings.</h1><p>Your dates, booking hold and payment result are saved. All current places and payments are for practice only.</p><aside className="testGateway"><b>Practice bookings</b><p>“Confirm practice payment” shows how payment works. It never asks for card details or uses real money.</p></aside>{status&&<p className="formStatus" aria-live="polite">{status}</p>}{bookings.length?<div className="bookingList">{bookings.map(b=><article className="bookingCard" key={b.id}><div className="bookingImage" style={{backgroundImage:`url(${b.imageUrl})`}}/><div><header><span className={`bookingStatus ${b.status}`}>{b.status}</span>{b.isTestData?<em>TEST DATA</em>:null}</header><small>{b.destination}</small><h2>{b.listingName}</h2><b>{b.startDate} → {b.endDate} · {b.guestCount} guest{b.guestCount===1?"":"s"}</b><p>Reference {b.reference}</p><div className="bookingTotal"><span>{b.paymentStatus?`Payment: ${b.paymentStatus}`:b.status==="held"?`Hold expires ${new Date(b.holdExpiresAt||"").toLocaleTimeString()}`:"No payment"}</span><strong>K{b.total} {b.currency}</strong></div><footer>{b.status==="held"&&b.isTestData?<button onClick={()=>confirm("Try the practice payment? No real money will be charged.")&&act("testConfirm",b.id)}>Confirm practice payment</button>:null}{["held","confirmed"].includes(b.status)?<button className="secondary" onClick={()=>confirm("Cancel this booking?")&&act("cancel",b.id)}>Cancel booking</button>:null}</footer></div></article>)}</div>:<div className="empty compact"><h2>No bookings yet</h2><p>Open a catalogue listing and choose “Check dates & book” to begin.</p></div>}</section>}function Stars({value,onChange,label="Rating"}:{value:number;onChange?:(n:number)=>void;label?:string}){return <div className="stars" aria-label={`${label}: ${value} out of 5`}>{[1,2,3,4,5].map(n=><button type="button" key={n} aria-label={`${n} ${n===1?"star":"stars"}`} className={n<=value?"filled":""} disabled={!onChange} onClick={()=>onChange?.(n)}>★</button>)}</div>}
function ReviewSummary({listingId}:{listingId:number}){const[data,setData]=useState<{reviews:PublicReview[];summary:{count:number;average:number}}|null>(null);useEffect(()=>{fetch(`/api/reviews?listingId=${listingId}`).then(r=>r.json()).then(setData).catch(()=>{})},[listingId]);if(!data)return <div className="reviewSummary">Loading traveller reviews…</div>;return <section className="reviewSummary"><div className="reviewScore"><strong>{data.summary.average||"New"}</strong><div>{data.summary.count?<Stars value={Math.round(data.summary.average)}/>:<span>No published reviews yet</span>}<small>{data.summary.count} traveller review{data.summary.count===1?"":"s"}</small></div></div>{data.reviews.slice(0,3).map(r=><article key={r.id}><header><b>{r.author}</b><span>{r.verificationType==="verified_booking"?"✓ Verified booking":"Unverified visit"}</span></header><Stars value={r.overallRating}/><h3>{r.title}</h3><p>{r.body}</p>{r.providerResponse&&<aside><b>Business reply</b><p>{r.providerResponse}</p></aside>}</article>)}</section>}
function ReviewSheet({listing,close,done}:{listing:Listing;close:()=>void;done:()=>void}){const[form,setForm]=useState({overallRating:5,valueRating:5,serviceRating:5,safetyRating:5,accessibilityRating:5,title:"",body:"",travelType:"solo",dateOfExperience:""});const[status,setStatus]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();setStatus("Checking and saving your review…");const r=await fetch("/api/reviews",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({listingId:listing.id,...form})});const x=await r.json();if(r.ok){setStatus(x.status==="published"?(x.verified?"Published with a verified-booking badge.":"Published as an unverified visit."):"Saved for a careful review.");setTimeout(done,1000)}else setStatus(x.error||"Review could not be submitted.")};return <div className="overlay" onClick={close}><article className="sheet reviewSheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={close}>×</button><p className="eyebrow">WRITE A REVIEW</p><h2>{listing.name}</h2><aside className="reviewPolicy"><b>Truthful reviews build trust</b><p>A completed eligible booking earns a verified badge. Other visits are published as unverified. Personal information, abusive content, spam and threats are held or rejected.</p></aside><form onSubmit={submit}><label>Overall experience<Stars value={form.overallRating} onChange={overallRating=>setForm({...form,overallRating})}/></label><div className="ratingGrid"><label>Value<Stars value={form.valueRating} onChange={valueRating=>setForm({...form,valueRating})}/></label><label>Service<Stars value={form.serviceRating} onChange={serviceRating=>setForm({...form,serviceRating})}/></label><label>Safety<Stars value={form.safetyRating} onChange={safetyRating=>setForm({...form,safetyRating})}/></label><label>Accessibility<Stars value={form.accessibilityRating} onChange={accessibilityRating=>setForm({...form,accessibilityRating})}/></label></div><label>Review title<input required minLength={3} maxLength={100} value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Your experience<textarea required minLength={10} maxLength={1500} rows={5} value={form.body} onChange={e=>setForm({...form,body:e.target.value})}/></label><div className="reviewMeta"><label>Travel type<select value={form.travelType} onChange={e=>setForm({...form,travelType:e.target.value})}><option value="solo">Solo</option><option value="couple">Couple</option><option value="family">Family</option><option value="friends">Friends</option><option value="business">Business</option></select></label><label>Date of experience<input type="date" max={new Date().toISOString().slice(0,10)} value={form.dateOfExperience} onChange={e=>setForm({...form,dateOfExperience:e.target.value})}/></label></div><button>Submit honest review</button></form>{status&&<p className="formStatus" aria-live="polite">{status}</p>}</article></div>}
function ReviewsScreen({viewer}:{viewer:Viewer}){const[reviews,setReviews]=useState<MyReview[]>([]);const[status,setStatus]=useState("Loading your reviews…");const load=useCallback(()=>{if(!viewer.signedIn)return;fetch("/api/reviews").then(r=>{if(!r.ok)throw new Error();return r.json()}).then(x=>{setReviews(x.reviews||[]);setStatus("")}).catch(()=>setStatus("Reviews could not be loaded."))},[viewer]);useEffect(()=>load(),[load]);const remove=async(id:number)=>{const r=await fetch("/api/reviews",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({reviewId:id})});const x=await r.json();if(r.ok){setReviews(x.reviews);setStatus("Review removed.")}else setStatus(x.error||"Review could not be removed.")};if(!viewer.signedIn)return <section className="accountGuest"><div className="accountMark">★</div><p className="eyebrow">YOUR REVIEWS</p><h1>Share trusted traveller insight.</h1><p>Sign in to share reviews and see their progress.</p><a href={viewer.signInPath}>Sign in to review</a></section>;return <section className="saved reviewsPage"><p className="eyebrow">YOUR REVIEWS & REPUTATION</p><h1>Your traveller reviews.</h1><p>Shared reviews appear on place pages. If a review needs checking, you will see why while it stays private.</p><aside className="reviewPolicy"><b>Fair review checks</b><p>Honest negative feedback is welcome. We only check for abuse, private details, unwanted advertising, dishonesty and unrelated content.</p></aside>{status&&<p className="formStatus" aria-live="polite">{status}</p>}{reviews.length?<div className="myReviews">{reviews.map(r=><article key={r.id}><div className="reviewThumb" style={{backgroundImage:`url(${r.imageUrl})`}}/><div><header><span className={`moderation ${r.moderationStatus}`}>{r.moderationStatus}</span><em>{r.verificationType==="verified_booking"?"✓ Verified booking":"Unverified visit"}</em></header><small>{r.listingName}</small><Stars value={r.overallRating}/><h2>{r.title}</h2><p>{r.body}</p>{r.moderationReason&&<aside><b>Why this needs checking</b><p>{r.moderationReason}</p></aside>}<button className="removeReview" onClick={()=>confirm("Remove this review?")&&remove(r.id)}>Remove review</button></div></article>)}</div>:<div className="empty compact"><h2>No reviews yet</h2><p>Open a listing and choose “Write a review” to share your experience.</p></div>}</section>}function Details({listing:p,close,book,review}:{listing:Listing;close:()=>void;book:()=>void;review:()=>void}){return <div className="overlay" onClick={close}><article className="sheet" onClick={e=>e.stopPropagation()}><button className="close" onClick={close}>×</button><div className="detailPic" style={{backgroundImage:`url(${p.imageUrl})`}}><span>{p.tag}</span></div><div className="details"><em>PLACE DETAILS · SAMPLE LISTING</em><h2>{p.name}</h2><b>⌖ {p.destination}, {p.province} · ★ {p.rating} ({p.reviewCount})</b><p>{p.summary}</p><div className="record"><span>Business</span><strong>{p.providerName}</strong><span>Category</span><strong>{p.categoryName}</strong><span>Place code</span><strong>{p.slug}</strong></div><ReviewSummary listingId={p.id}/>{p.sourceUrl&&<a href={p.sourceUrl} target="_blank" rel="noreferrer">Visit the business website ↗</a>}<footer><div><small>Sample price</small><strong>K{p.memberPrice??p.basePrice}</strong></div><div className="detailActions"><button className="secondary" onClick={review}>Write a review</button><button onClick={book}>Check dates & book</button></div></footer></div></article></div>}
function Loading(){return <div className="loading">{[1,2,3].map(x=><div key={x}><span/><i/><i/></div>)}</div>}function ErrorState({message,retry}:{message:string;retry:()=>void}){return <div className="empty"><h2>Places are unavailable</h2><p>{message}</p><button onClick={retry}>Try again</button></div>}function Empty({clear}:{clear:()=>void}){return <div className="empty"><h2>No exact matches</h2><p>Try another destination, province or category.</p><button onClick={clear}>Clear filters</button></div>}function ModuleStatus({openReviews}:{openReviews:()=>void}){return <aside className="member module"><div><p className="eyebrow lime">YOUR REVIEWS & REPUTATION</p><h2>Share the journey honestly</h2><p>Rate real experiences, see confirmed-visit badges and follow each review clearly.</p><button onClick={openReviews}>Manage reviews</button></div><strong>07</strong></aside>}
function Bottom({tab,setTab}:{tab:string;setTab:(t:"Explore"|"Bookings"|"Saved"|"Trips"|"Profile")=>void}){return <nav className="bottom"><button className={tab==="Explore"?"active":""} onClick={()=>setTab("Explore")}><span>⌂</span>Explore</button><button className={tab==="Bookings"?"active":""} onClick={()=>setTab("Bookings")}><span>▤</span>Bookings</button><button className={tab==="Saved"?"active":""} onClick={()=>setTab("Saved")}><span>♡</span>Saved</button><button className={tab==="Trips"?"active":""} onClick={()=>setTab("Trips")}><span>▣</span>Trips</button><button className={tab==="Profile"?"active":""} onClick={()=>setTab("Profile")}><span>○</span>Profile</button></nav>}










