import {NextResponse} from "next/server";import {cookies} from "next/headers";import {createHash} from "node:crypto";import {env} from "../../db/runtime";import {safeReturnPath} from "../auth";
export async function GET(request:Request){const jar=await cookies(),token=jar.get("visitpng_session")?.value;if(token)await env.DB.prepare("DELETE FROM auth_sessions WHERE token_hash=?").bind(createHash("sha256").update(token).digest("hex")).run();jar.delete("visitpng_session");return NextResponse.redirect(new URL(safeReturnPath(new URL(request.url).searchParams.get("return_to")||"/"),request.url))}
