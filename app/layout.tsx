import type {Metadata} from "next";import "./theme.css";import "./globals.css";export const metadata:Metadata={title:"Visit PNG — Discover Papua New Guinea",description:"Discover verified places, stays and experiences across Papua New Guinea.",icons:{icon:"/favicon.svg"}};export default function RootLayout({children}:Readonly<{children:React.ReactNode}>){return <html lang="en"><body>{children}</body></html>}
